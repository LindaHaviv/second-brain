# Copy to .env (gitignored). Used by docker-compose and the agent.
ORACLE_PWD=DemoPwd_2026      # SYSTEM/SYS password
APP_PWD=CccPwd_2026          # CCC app-schema password

# DB connection for the Python agent
DB_USER=CCC
DB_DSN=localhost:1521/FREEPDB1

# LLM for the agent loop (Claude)
ANTHROPIC_API_KEY=sk-ant-...
